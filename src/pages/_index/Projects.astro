---
import ProjectCard from '@components/ui/ProjectCard.astro'
import VoidProject from '@components/ui/VoidProject.astro'
---

<section class="projects">
  <h2 class="projects-title"><span>[</span>Proyectos<span>]</span></h2>
  <p>
    A lo largo del tiempo he creado distintos proyectos como freelancer, acá
    encontrarás una recopilación de mis <strong>proyectos más recientes</strong
    >.
  </p>
  <article class="cards-container">
    <ProjectCard />
    <VoidProject placeholder rotatingHeader />
  </article>
</section>

<style>
  .projects {
    margin-top: 6rem;
    & .projects-title {
      letter-spacing: 0.15rem;

      & span {
        color: var(--blue);
      }
    }
  }
  .cards-container {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 24px;
    margin: 3rem 0;
  }

  @media (width <= 750px) {
    .cards-container {
      grid-template-columns: 1fr;
      margin: 2rem 0;
    }
  }
</style>

<script src="@scripts/shuffle-text.js"></script>
